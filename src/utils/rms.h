#ifndef RMS_H
#define RMS_H

#include <bits/stdc++.h>
#include "math.h"
using namespace std;

double get_rms(const vector<double> *p_vect);
double get_rms(const vector<vector<double>> *p_vect);

#endif
